# =============================================================================
# Docker Environment Variables (LOCAL DEVELOPMENT ONLY)
# =============================================================================
# Copy this file to .env.docker and fill in with your own values
# DO NOT commit .env.docker to git!
# =============================================================================

# PostgreSQL credentials for local Docker development
POSTGRES_DB=financa_dev
POSTGRES_USER=financa_dev
POSTGRES_PASSWORD=your_secure_local_password_here
