// Componente legado — substituído pelo Bottom Tab Navigator em (tabs)/_layout.tsx
import React from 'react';
import { View } from 'react-native';
export default function AppTabs() { return <View />; }
export function TabButton() { return <View />; }
export function CustomTabList() { return <View />; }
